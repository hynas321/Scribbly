import Hub from "./Hub";
import HubEvents from "./HubEvents";

class HubHandler {
    private hub: Hub

    constructor(hub: Hub) {
        this.hub = hub;
    }

    loadGameSettings(token: string, gameHash: string) {

    }
}