class EndpointHandler {
    createGame(
        endpointUrl: string,
        hostUsername: string,
        gameSettings: object) {
        console.log(`TODO, sent data to ${endpointUrl}:\n
         hostUsername: ${hostUsername}\n
         gameSettings: ${gameSettings}`);
    }
}

export default EndpointHandler